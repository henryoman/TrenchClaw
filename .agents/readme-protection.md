# README Protection

Do not delete, remove, prune, rename away, or replace `README.md` unless the user explicitly asks for that exact change in the current conversation.

If a requested change would remove `README.md` as a side effect, stop and ask first.

If `README.md` is missing unexpectedly, restore the most recent real version instead of rewriting it from memory or choosing an older revision without checking.
