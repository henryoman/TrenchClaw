# @trenchclaw/core

To install dependencies:

```bash
bun install
```

To run core runtime + CLI from repo root:

```bash
bun run dev
```

To run package-local checks:

```bash
bun run typecheck
bun run test
```
