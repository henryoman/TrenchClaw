# Human/operator-editable override layer.
# Final authority for protected keys.

observability:
  logging:
    level: info
    style: human
    pretty: true

ui:
  webGui:
    host: 127.0.0.1
    port: 4173
