# Agent/runtime-editable override layer.
# Only allowlisted low-risk keys are applied from this file.
# User-protected keys (wallet/trading permissions, dangerous flags, network authority)
# are ignored even if set here.

runtime:
  scheduler:
    tickMs: 1000
    maxConcurrentJobs: 8

triggers:
  enabled: true
