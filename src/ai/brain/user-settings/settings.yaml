configVersion: 1

rpc:
  primaryRpc: helius
  providers:
    helius:
      endpointRef: vault://rpc/helius/http-url
      wsEndpointRef: vault://rpc/helius/ws-url
      apiKeyRef: vault://rpc/helius/api-key

trading:
  swapSettings: swap.yaml
  preferredSwap: ultra
  defaultSwapProfile: ultra
  slippagePresetBps: ultra
  sizing:
    maxTradeSize: 1 #sol
    maxTradesPerHour: 10


wallet:
  activeWalletRef: vault://wallets/main/active-address
  signerRef: vault://wallets/main/signer

integrations:
  dexscreenerApiKeyRef: vault://integrations/dexscreener/api-key
  jupiterApiKeyRef: vault://integrations/jupiter/api-key

notifications:
  config: notications.yaml
