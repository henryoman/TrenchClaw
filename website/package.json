{
  "name": "trenchclaw",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.10",
  "engines": {
    "bun": ">=1.3.10",
    "node": "^20.19.0 || >=22.12.0"
  },
  "type": "module",
  "scripts": {
    "launch": "bun run --cwd .. launch",
    "content:sync": "bun run ./scripts/sync-shared-content.ts",
    "dev": "bun run content:sync && bunx --bun vite dev",
    "build": "bun run content:sync && bunx --bun vite build",
    "preview": "bun run content:sync && bunx --bun vite preview",
    "lint": "cd .. && bunx oxlint -c .oxlintrc.json website",
    "test": "bun test ../tests/website",
    "svelte:check": "bun run content:sync && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check": "bun run content:sync && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "bun run content:sync && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "typecheck": "bun run content:sync && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "ci": "bun run lint && bun run test && bun run svelte:check && bun run typecheck && bun run build"
  },
  "dependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.55.0",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "highlight.js": "^11.11.1",
    "marked": "^17.0.4",
    "svelte": "^5.54.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "oxlint": "^1.56.0",
    "svelte-check": "^4.4.5",
    "tailwindcss": "^4.2.2",
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  }
}
