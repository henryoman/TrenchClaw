# GUI-only local launcher settings (optional)
RUNTIME_HOST=127.0.0.1
RUNTIME_PORT=4020
GUI_PORT=4173

# Runtime mode
TRENCHCLAW_PROFILE=dangerous

# API keys should NOT be placed here.
# Store secrets in:
# apps/trenchclaw/src/ai/brain/protected/no-read/vault.json
